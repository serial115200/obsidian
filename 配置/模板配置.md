# 模板

在 `模板/` 文件夹中存放预设的笔记骨架，新建笔记时通过 `Ctrl+P` → "插入模板" 快速套用。

模板支持以下内置变量：

| 变量 | 说明 |
|------|------|
| `{{date}}` | 当前日期 |
| `{{time}}` | 当前时间 |
| `{{title}}` | 笔记文件名 |

## 设置路径

设置 → 核心插件 → 模板 → ⚙️

- **模板文件夹位置**：`模板/`
